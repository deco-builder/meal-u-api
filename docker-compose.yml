version: '3.9'

services:
  server:
    image: nafisazizir/meal-u-prod:latest
    restart: unless-stopped
    # command: gunicorn --bind 0.0.0.0:$PORT myproject.wsgi:application
    ports:
      - "8000:8000"
    env_file:
      - .env.prod

volumes:
  postgres_data:
